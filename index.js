import Server from './models/server.js'; // Asegúrate de que la ruta sea correcta

const server = new Server(); // Crea una instancia de la clase Server
server.listen(); // Llama al método listen() para iniciar el servidor
